// The package compiler certifies that the consumer can resolve and consume the
// installed public contract package without importing provider implementation.
export { counterService } from "@company/counter-base";
