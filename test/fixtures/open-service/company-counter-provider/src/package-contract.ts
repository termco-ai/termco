// The package compiler certifies that the provider can resolve and consume the
// installed public contract package without importing consumer implementation.
export { counterService } from "@company/counter-base";
