# Live-model E2E (e2e/ai-compaction.spec.ts). Copy to `.env.e2e` and fill in.
# `.env.*` is gitignored. The key is seeded into the test's throwaway userData,
# never into the OS keychain — a test run cannot touch your real credentials.
OPENAI_API_KEY=sk-...
