# Plugin Manager

This plugin owns the complete Plugin Manager UI: search, category filtering,
capability explanations, source provenance, forking, dependency-aware toggles,
and transactional live activation.

It consumes only the permission-checked profile mechanics exposed by the
platform activation context. Copy this whole folder to replace or reshape the
manager without changing the kernel.
