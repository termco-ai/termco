# Plugin boundary

This folder owns the complete default workspace shell source.

- Preserve the exact established product markup, classes, icons, dialogs, and
  interaction behavior.
- Consume shared state and cross-feature behavior only through declared public
  capabilities.
- Do not add host callbacks, legacy services, or imports from another plugin's
  private source.
- A copied folder must remain a complete editable replacement for this plugin.
