# Safe Profile Recovery

This renderer plugin owns the recovery notice shown after a profile boot
failure. It reads the selected diagnostic provider and opens the normal
plugin-owned Settings/Plugin Manager UI; the kernel has no hidden recovery UI.
