# Boot Diagnostics

This provider owns the durable diagnostic created when profile startup falls
back to the protected recovery profile. The kernel selects profiles and calls
the public capability; it does not own recovery presentation or storage.
