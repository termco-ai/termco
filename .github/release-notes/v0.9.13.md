- Keep expanded Chat and Agents, tab docking previews, and pane drag previews above native browser content.
- Keep browser pages aligned with their panes when moving them without changing their size.
- Restore normal workspace backgrounds when browser tabs are hidden, empty, or failed.
- Paint split headers and terminal surfaces opaquely beside a live browser, preventing old UI content from showing through after chat and pane moves.
- Verify repeated docking, full-width chat resizing, updating conversations, and native browser layering using the production renderer before publishing the Mac release.
- Prepare the complete plugin profile for the release tests and preserve startup diagnostics on failure. The 0.9.12 application release was withheld after this gate exposed a missing test-build prerequisite.
- The matching plugin snapshot requires Termco 0.9.12 or newer and is compatible with 0.9.13.
