- Arrange files and terminals above, below, left, or right with drag previews and the new tab layout picker.
- Move an already docked pane again by dragging its title bar, preserving terminal sessions, unsaved edits, and undo history.
- Open a terminal below a file in that file's directory, and restore pane orientation and order with the workspace.
- Expand Chat and Agents to the left sidebar and resize them back; the full-width setting follows window and sidebar changes.
- Include the updated workspace shell and public docking interfaces required by the matching plugin release. That plugin snapshot requires Termco 0.9.11 or newer.
