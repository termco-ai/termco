- Keep expanded Chat and Agents, tab docking previews, and pane drag previews above native browser content.
- Keep browser pages aligned with their panes when moving them without changing their size.
- Restore normal workspace backgrounds when browser tabs are hidden, empty, or failed.
- Paint split headers and terminal surfaces opaquely beside a live browser, preventing old UI content from showing through after chat and pane moves.
- Test native browser layering, repeated docking, chat resizing, and updating conversations using the production renderer before publishing the Mac release.
- The matching plugin snapshot requires Termco 0.9.12 or newer.
